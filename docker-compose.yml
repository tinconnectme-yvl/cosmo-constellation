version: '3.8'

services:
  satellite-constellation-app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: kosmohack-constellation-service
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/health"]
      interval: 15s
      timeout: 5s
      retries: 3
